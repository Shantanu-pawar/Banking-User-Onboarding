# src/test/resources/application.yaml

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/banking_user_onboarding
    username: root
    password: adminuser
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
